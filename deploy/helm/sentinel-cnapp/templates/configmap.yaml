apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sentinel-cnapp.fullname" . }}-config
data:
  sentinel.service.name: "sentinel"
  sentinel.service.version: "0.1.0"
  sentinel.log.level: "{{ .Values.assetInventory.config.sentinelLogLevel }}"
  sentinel.log.format: "json"
