apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sentinel-cnapp.fullname" . }}-scanner-container
spec:
  replicas: {{ .Values.scannerContainer.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/component: scanner-container
  template:
    metadata:
      labels:
        app.kubernetes.io/component: scanner-container
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      containers:
        - name: scanner-container
          image: "{{ .Values.scannerContainer.image.repository }}:{{ .Values.scannerContainer.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SENTINEL_SERVICE_NAME
              value: "sentinel-scanner-container"
            - name: SENTINEL_SERVICE_PORT
              value: "8080"
            - name: SENTINEL_NATS_URL
              value: "nats://{{ .Release.Name }}-nats:4222"
            - name: SENTINEL_LOG_LEVEL
              value: "{{ .Values.scannerContainer.config.sentinelLogLevel }}"
          resources: {{ toYaml .Values.scannerContainer.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel-cnapp.fullname" . }}-scanner-container
spec:
  selector:
    app.kubernetes.io/component: scanner-container
  ports:
    - port: 8080
      targetPort: http
      name: http
