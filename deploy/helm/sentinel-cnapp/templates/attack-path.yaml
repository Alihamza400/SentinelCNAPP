apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sentinel-cnapp.fullname" . }}-attack-path
spec:
  replicas: {{ .Values.attackPath.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/component: attack-path
  template:
    metadata:
      labels:
        app.kubernetes.io/component: attack-path
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      containers:
        - name: attack-path
          image: "{{ .Values.attackPath.image.repository }}:{{ .Values.attackPath.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SENTINEL_SERVICE_NAME
              value: "sentinel-attack-path"
            - name: SENTINEL_SERVICE_PORT
              value: "8080"
            - name: SENTINEL_NEO4J_URI
              value: "bolt://{{ .Release.Name }}-neo4j:7687"
            - name: SENTINEL_NEO4J_USER
              value: "neo4j"
            - name: SENTINEL_NEO4J_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "sentinel-cnapp.fullname" . }}-neo4j
                  key: password
            - name: SENTINEL_LOG_LEVEL
              value: "{{ .Values.attackPath.config.sentinelLogLevel }}"
          resources: {{ toYaml .Values.attackPath.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel-cnapp.fullname" . }}-attack-path
spec:
  selector:
    app.kubernetes.io/component: attack-path
  ports:
    - port: 8080
      targetPort: http
      name: http
